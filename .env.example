# Optional: enables the AI enrichment layer (semantic findings on top of the
# deterministic rule engine). Without a key the tool runs fully offline.
# Copy this file to `.env` and fill in your key. The .env file is gitignored.
ANTHROPIC_API_KEY=
